export interface ISignIn {
  displayName: string;
  email: string;
  expiresIn: string;
  idToken: string;
  kind: string;
  localId: string;
  registered: boolean;
  refreshToken: string;
  verified: boolean;
}